apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: demo-pos-mvp
spec:
  selector:
    matchLabels:
      app: demo-pos-mvp # has to match .spec.template.metadata.labels
  serviceName: "demo-pos-mvp"
  replicas: 2 # by default is 1
  updateStrategy:
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        app: demo-pos-mvp # has to match .spec.selector.matchLabels
    spec:
      shareProcessNamespace: false
      terminationGracePeriodSeconds: 10
      restartPolicy: Always
      hostAliases:
      - ip: "172.31.106.149"
        hostnames:
        - "db"
      containers:
      - name: c226-mvp
        image: magestore/apache-php:7.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        - containerPort: 443
        securityContext:
          allowPrivilegeEscalation: true
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/html
          subPath: magento_c226_mvp
        lifecycle:
          postStart:
            exec:
              command: ["sh", "-c", "chown -R ubuntu:ubuntu /var/www/html"]
      - name: c226-mvp-init
        image: magestore/magento:2.2.6-mvp
        imagePullPolicy: Always
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/mvp
          subPath: magento_c226_mvp
        command: ["sh", "-c", "while true; do echo 1; sleep 1; done"]
        lifecycle:
          postStart:
            exec:
              command:
              - "sh"
              - "-c"
              - >
                cp -dRf /var/www/html/. /var/www/mvp/ ;
                chown -R ubuntu:ubuntu /var/www/mvp ;
      volumes:
      - name: demo-data
        persistentVolumeClaim:
          claimName: demo-nfs
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: demo-pos-aus
spec:
  selector:
    matchLabels:
      app: demo-pos-aus # has to match .spec.template.metadata.labels
  serviceName: "demo-pos-aus"
  replicas: 2 # by default is 1
  updateStrategy:
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        app: demo-pos-aus # has to match .spec.selector.matchLabels
    spec:
      shareProcessNamespace: false
      terminationGracePeriodSeconds: 10
      restartPolicy: Always
      hostAliases:
      - ip: "172.31.106.149"
        hostnames:
        - "db"
      containers:
      - name: c226-aus
        image: magestore/apache-php:7.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        - containerPort: 443
        securityContext:
          allowPrivilegeEscalation: true
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/html
          subPath: magento_c226_aus
        lifecycle:
          postStart:
            exec:
              command: ["sh", "-c", "chown -R ubuntu:ubuntu /var/www/html"]
      - name: c226-aus-init
        image: magestore/magento:2.2.6-aus
        imagePullPolicy: Always
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/aus
          subPath: magento_c226_aus
        command: ["sh", "-c", "while true; do echo 1; sleep 1; done"]
        lifecycle:
          postStart:
            exec:
              command:
              - "sh"
              - "-c"
              - >
                cp -dRf /var/www/html/. /var/www/aus/ ;
                chown -R ubuntu:ubuntu /var/www/aus ;
      volumes:
      - name: demo-data
        persistentVolumeClaim:
          claimName: demo-nfs
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: demo-pos-omc
spec:
  selector:
    matchLabels:
      app: demo-pos-omc # has to match .spec.template.metadata.labels
  serviceName: "demo-pos-omc"
  replicas: 2 # by default is 1
  updateStrategy:
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        app: demo-pos-omc # has to match .spec.selector.matchLabels
    spec:
      shareProcessNamespace: false
      terminationGracePeriodSeconds: 10
      restartPolicy: Always
      hostAliases:
      - ip: "172.31.106.149"
        hostnames:
        - "db"
      containers:
      - name: c226-omc
        image: magestore/apache-php:7.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        - containerPort: 443
        securityContext:
          allowPrivilegeEscalation: true
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/html
          subPath: magento_c226_omc
        lifecycle:
          postStart:
            exec:
              command: ["sh", "-c", "chown -R ubuntu:ubuntu /var/www/html"]
      - name: c226-omc-init
        image: magestore/magento:2.2.6-omc
        imagePullPolicy: Always
        volumeMounts:
        - name: demo-data
          mountPath: /var/www/omc
          subPath: magento_c226_omc
        command: ["sh", "-c", "while true; do echo 1; sleep 1; done"]
        lifecycle:
          postStart:
            exec:
              command:
              - "sh"
              - "-c"
              - >
                cp -dRf /var/www/html/. /var/www/omc/ ;
                chown -R ubuntu:ubuntu /var/www/omc ;
      volumes:
      - name: demo-data
        persistentVolumeClaim:
          claimName: demo-nfs
